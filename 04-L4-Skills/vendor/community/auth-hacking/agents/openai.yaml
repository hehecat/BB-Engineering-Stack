interface:
  display_name: "Auth Hacking"
  short_description: "Authentication bypass testing methodology"
  default_prompt: "Use $auth-hacking to map authentication bypass risk across the provided auth flows, app context, or HTTP/API traffic."
