# BB Stack Runtime Pointer

The active security workflow is injected by the managed runtime Prompt files.
Use `bb-claude` for per-engagement profile selection and `bb-stack keysmith
status` before relying on a persistent raw `claude` wrapper.
